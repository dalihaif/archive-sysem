## 2026-06-23 修复档案管理平台问题

### 问题诊断
- 27个Python文件语法全部通过
- /catalog/import 路由实际正确地址是 /import/（import_bp 注册的 url_prefix="/import"），之前测试路径有误，非真实bug
- /catalog/export 超时：原因是对27896条数据逐行设置4个单元格属性（font/border/alignment/fill），约56万次操作

### 修复内容（catalog/routes.py）
1. **导出限制**：加 EXPORT_LIMIT=5000，超出时末尾写红色提示行
2. **性能优化**：数据行从逐格设置属性改为 ws.append(row_data)，去掉边框+隔行背景（仅保留表头样式）
3. **清理无用导入**：删除 Border、Side（已不使用）
4. 优化后耗时：1.54s（前：超时）；文件大小：~986KB（5000条）